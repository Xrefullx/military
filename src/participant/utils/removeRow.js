export const removeRow = (tableData, setTableData, index) => {
    if (tableData.length > 1) {
        setTableData((prevData) => prevData.filter((_, i) => i !== index));
    }
};
export const removeRow2 = (tableData2, setTableData2,index) => {
    if (tableData2.length > 1) {
        setTableData2((prevData) => prevData.filter((_, i) => i !== index));
    }
};
export const removeRow3 = (tableData3, setTableData3,index) => {
    if (tableData3.length > 1) {
        setTableData3((prevData) => prevData.filter((_, i) => i !== index));
    }
};

export const removeRow4 = (tableData4, setTableData4,index) => {
    if (tableData4.length > 1) {
        setTableData4((prevData) => prevData.filter((_, i) => i !== index));
    }
};

export const removeRow5 = (tableData5, setTableData5,index) => {
    if (tableData5.length > 1) {
        setTableData5((prevData) => prevData.filter((_, i) => i !== index));
    }
};

export const removeRow6 = (tableData6, setTableData6,index) => {
    if (tableData6.length > 1) {
        setTableData6((prevData) => prevData.filter((_, i) => i !== index));
    }
};

export const removeRow7 = (tableData7, setTableData7,index) => {
    if (tableData7.length > 1) {
        setTableData7((prevData) => prevData.filter((_, i) => i !== index));
    }
};

export const removeRow8 = (tableData8, setTableData8,index) => {
    if (tableData8.length > 1) {
        setTableData8((prevData) => prevData.filter((_, i) => i !== index));
    }
};